# Steward suggestions

> Auto-generated by `steward doctor` on 2026-06-13.
> Re-run `steward doctor --scope siblings` to refresh.
> See `docs/perfect-patient.md` in the steward checkout for the corpus baseline.

Repo: `dgx-spark-cli` — 1 agent(s) declared.

## Repo-level findings

- **portability** `.`: ❌ Per-user ~/.<dotfile> config refs in committed doc/config:
    CHANGELOG.md:55:- Watches memory %, swap %, disk %, hottest sensor, GPU temp, load-per-core, container health, and subsystem availability (nvidia-smi/docker going dark). Thresholds and the webhook live in `~/.config/dgx-spark/monitor.json` (a `null` threshold disables that check); `DGX_SPARK_WEBHOOK_URL` overrides the webhook.
    README.md:73:dgx-spark-cli monitor config --init     # scaffold ~/.config/dgx-spark/monitor.json
   Allowed carve-outs: ~/.claude/skills/.../scripts/ (tool calls), ~/.culture/ (mesh data).
   Otherwise: commit a repo-local config or document a portable lookup.

## Agent: `dgx-spark-cli`

- Backend: `claude`

_No baseline findings — this agent matches the corpus._
